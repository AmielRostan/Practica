<div class="collapse navbar-collapse show" id="navbarWithDropdown" style="float: right; margin-left:600px">
  <ul class="navbar-nav">
    <% if user_signed_in? %>
      <a class="btn-secondary" style="border-radius: 5px; padding-top: 10px; padding-left:8px; padding-right:8px">
        <span style="color:white"><strong><%= current_user.last_name %>, </strong><%= current_user.first_name %></span>
      </a>
      <%= link_to "Editar Perfil", edit_user_registration_path, class: "btn" %>
      <%= link_to "Cerrar Sesión", destroy_user_session_path, class: "btn",  method: :delete  %>
    <% else %>
      <%= link_to "Iniciar Sesión", new_user_session_url, class: 'btn btn-success' %>
    <% end %>
  </ul>
</div>
